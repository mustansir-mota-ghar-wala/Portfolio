PORT=8000

# One origin, or several comma-separated origins (no trailing slash).
# Use "*" only for local experiments.
# Local development:
FRONTEND_ORIGIN=http://localhost:5173
# Production (Vercel) example — keep the localhost entry if you develop against it:
# FRONTEND_ORIGIN=https://your-portfolio.vercel.app,http://localhost:5173
